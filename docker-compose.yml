services:
    web-app:
        container_name: blazesoft-interview-web-app
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "3000:3000"
        command: node server.js
